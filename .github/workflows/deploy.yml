name: Deploy to Docker on Ubuntu Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to server
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.194.164.60.164 }}
          username: ${{ secrets.root }}
          key: ${{ secrets.wiki_key }}
          script: |
            cd /path/to/project
            git pull origin main
            docker build -t wiki_app .
            docker stop wiki_app || true
            docker rm wiki_app || true
            docker run -d -p 8080:8080 --name wiki_app wiki_app
